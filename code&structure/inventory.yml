---
all:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ~/.ssh/aws-key.pem
    ansible_python_interpreter: /usr/bin/python3

  children:
    monitoring:
      hosts:
        monitoring-server:
          ansible_host: <ip>
          private_ip: <ip>

    targets:
      hosts:
        target-server-1:
          ansible_host: <ip>
          private_ip: <ip>
        target-server-2:
          ansible_host: <ip>
          private_ip: <ip>